import pytest
from playwright.sync_api import sync_playwright

# pytest-playwright handles the `page` fixture automatically.
# This file is kept for any future custom fixtures (e.g., authenticated context).
